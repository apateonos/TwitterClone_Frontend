import React from 'react';
import { Navigation } from '../../components/index';

export default () => {
  return (
    <header>
      <Navigation/>
      <div>
        <button>settings</button>
      </div>
    </header>
  )
}